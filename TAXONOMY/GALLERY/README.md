# Gallery

Students can place training images in this folder.

Important: after adding a file, also add a matching entry in `../data/specimens.js`; otherwise a static browser page cannot discover the image automatically.
